	\RequirePackage{ifpdf} 	% для раздельной компиляции pdf и dvi 
	
	\newcommand{\STYLES}{\globalStylesDir}	
	\newcommand{\IMG}{\globalPictureDir}
	\newcommand{\SRC}{\globalSourceDir}
	
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%% жизненноважная директива
%%%
	
	\usepackage[\globalEncoding]{inputenc}							% кодировка, тут очень аккуратно
		%% вариант:
		%	\usepackage[utf8x]{inputenc}
		%	\usepackage{ucs}
		
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%% опишем <<наши>> модули, раздельное описание --- легче редактировать
%%%
	
	\usepackage{\globalStylesDir/myFonts}		% наши шрифты и локализация
	\usepackage{\globalStylesDir/myMath}		% модуль нашей математики 
	\usepackage{\globalStylesDir/myGraphics}	% модуль нашей графики
	\usepackage{\globalStylesDir/myCode}		% модуль наших листингов
	\usepackage{\globalStylesDir/myTables} 		% модуль наших таблиц
	\usepackage{\globalStylesDir/myColRows}		% модуль нашей многополосности
	\usepackage{\globalStylesDir/myQuotes}		% модуль наших цитат
	\usepackage{\globalStylesDir/myTheorem}		% модуль наших теоремм и определений
	\usepackage{\globalStylesDir/myAlgorithms}	% модуль наших алгоритмов
	\usepackage{\globalStylesDir/myLscape}		% модуль нашией ориентации (книжная, албомная)
	\usepackage{\globalStylesDir/myHyperref}	% модуль нашиих гиперссылок
	\usepackage{\globalStylesDir/myAppendix}    % модуль наших приложений
		
	%% это уже косметика, но мы тоже их вынесем отдельно
	\usepackage{\globalStylesDir/myLists} 		% модуль наших списков
												% переопределение буллетов 
	\usepackage{\globalStylesDir/mySections} 	% модуль наших секций
												% настройка отображения частей 	
	\usepackage{\globalStylesDir/userCommands}	% всевозможные переопределения
												% настройка отображения частей 	

	
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%% настройка параметров документа
%%%
%	\oddsidemargin=-0.4mm
%	\textwidth=160mm
%	\topmargin=4.6mm
%	\textheight=210mm
%	\parindent=1.25cm
%	\parskip=3pt
	\usepackage{extsizes}
	
	\usepackage{geometry}
	\geometry{left=1.5cm}
	\geometry{right=1.5cm}
	\geometry{top=1.5cm}
	\geometry{bottom=1.5cm}
	
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%% настройка нумерации
%%%
	\usepackage{fancyhdr}
	\pagestyle{fancy}
	\fancyhf{}
	\fancyfoot[C]{\thepage}
	\fancyheadoffset{0mm}
	\fancyfootoffset{1000mm}
	\setlength{\headheight}{17pt}
	\renewcommand{\headrulewidth}{0pt}
	\renewcommand{\footrulewidth}{0pt}
	\fancypagestyle{plain}{ 
    		\fancyhf[C]{}
    		\cfoot{\thepage}}
    \setcounter{page}{2} % начать нумерацию страниц с №2