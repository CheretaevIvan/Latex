%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%% подключение цветов и графики (желательно сохранять именно такой порядок)
%%%

% -------------------------------------------------------
% Картинки
% -------------------------------------------------------

	\usepackage{float}
	\ifpdf 
		\usepackage[pdftex]{graphicx} 
	\else
		\usepackage{graphicx}
			% вообще говоря, тут возникли некоторые проблемы (Common LaTeX):
			% 	Cannot determine size of graphic in *.??? (no BoundingBox).
			% и в dvi картинки стали ОГРОМНЫМИ 
			% для нормального растра (jpg, png) надо использовать конверторы (jpg2ps)
	\fi
	
% -------------------------------------------------------
% Цвета
% -------------------------------------------------------	

	\usepackage[usenames]{xcolor}
	
% -------------------------------------------------------
% Векторные рисунки
% -------------------------------------------------------	
	\ifpdf \else
		\usepackage{pspicture}
		\usepackage{pst-plot,pstricks-add}
		%\usepackage{pstricks,pst-node,pst-text,pst-3d,pst-grad}	
	\fi

	\usepackage{tikz}
		\usetikzlibrary{arrows,shapes,shadows,decorations}
		\usetikzlibrary{decorations.pathmorphing}
		\usetikzlibrary{calc}

	\usepackage[tableposition=top]{caption}
	\usepackage{subcaption}
	
	\DeclareCaptionLabelFormat{gostfigure}{Рисунок #2}
	\DeclareCaptionLabelFormat{gosttable}{Таблица #2}
	\DeclareCaptionLabelSeparator{gost}{.~}
	\captionsetup{
		labelsep=gost,
		justification=centering,
		textformat=period
		}
	\captionsetup[figure]{
		labelformat=gostfigure,
		figurewithin=none
		}
	\captionsetup[table]{
		labelformat=gosttable,
		tablewithin=none
		}
	\renewcommand{\thesubfigure}{\asbuk{subfigure}}
	\newcommand{\graphic}[3]{
		\begin{figure}[H]
			\centering
			\includegraphics[scale=#2]{\IMG/#1}
			\caption{#3}
			\label{pic:#1}
		\end{figure}
	}
	
		

